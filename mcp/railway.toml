[build]
builder = "nixpacks"
buildCommand = "pip install --no-cache-dir -r requirements.txt"
nixpacksPlan = { phases = { setup = { nixPkgs = ["python311", "gcc"] } } }

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

[env]
PORT = "8000"
HOST = "0.0.0.0"
ENVIRONMENT = "production"